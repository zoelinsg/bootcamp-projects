# 252203 統神端火鍋

```python
# 讀入一行價格（逗號分隔）
line = input().strip()
price1 = list(map(int, line.split(',')))  # 實際端鍋順序

# 正確的端鍋順序：從最貴排到最便宜
price2 = sorted(price1, reverse=True)

# 計算端錯次數
k = 0
for i in range(len(price1)):
    if price1[i] != price2[i]:
        k += 1

print(f"過來一下，你又端錯{k}次了")
```