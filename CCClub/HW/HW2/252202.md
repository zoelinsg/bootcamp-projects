# 252202 雜湊函數

```python
n = int(input())
nums = list(map(int, input().split(',')))

ans = []

for num in nums:
    r = num % n              # 原本的餘數

    # 碰撞處理：如果這個餘數已經用過，就往後找下一個
    while r in ans:
        r = (r + 1) % n

    ans.append(r)

print(",".join(map(str, ans)))
```